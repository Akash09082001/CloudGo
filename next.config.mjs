/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: 'uncommon-bass-847.convex.cloud',
            },
        ],
    },
};

export default nextConfig;
